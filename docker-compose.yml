# =============================================================================
# ClinAuditAI — Docker Compose
# =============================================================================
# Usage:
#   docker compose up --build        # Start all services
#   docker compose down               # Stop all services
#   docker compose logs -f app        # Follow app logs

services:

  # -- Application --
  app:
    build: .
    container_name: clinauditai-app
    ports:
      - "${APP_PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      - DB_HOST=db
      - DB_PORT=5432
    depends_on:
      db:
        condition: service_healthy
    volumes:
      # Mount source code for hot-reload in development
      - ./src:/app/src:ro
      # Mount data directory for access to CSV/index files
      - ./data:/app/data:ro
      # Cache HuggingFace models so PubMedBERT isn't re-downloaded on every rebuild
      - hf_cache:/home/appuser/.cache/huggingface
    command: >
      uvicorn src.main:app
      --host 0.0.0.0
      --port 8000
      --reload
    restart: unless-stopped

  # -- Database --
  db:
    image: postgres:16-alpine
    container_name: clinauditai-db
    environment:
      POSTGRES_DB: ${DB_NAME:-clinaudit_ai}
      POSTGRES_USER: ${DB_USER:-gg_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme_in_production}
    ports:
      - "${DB_PORT:-5433}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-gg_user} -d ${DB_NAME:-clinaudit_ai}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres_data:
  hf_cache:
